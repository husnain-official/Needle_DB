struct addrinfo {
    int              ai_flags;          AI_passive = this is the server, not a client
    int              ai_family;         AF_UNSPEC(whatever works),  tells what type of IP to expect
    int              ai_socktype;       Stream of Dgram
    int              ai_protocol;       0 = choose default protocol for this socket
    size_t           ai_addrlen;        size of address to distinguish
    struct sockaddr *ai_addr;           the address to data itself(ip and port #)
    char            *ai_canonname;      
    struct addrinfo *ai_next;      // linked list, next node
};
    first 4 tells socket how to behave and what type of connection to expect
next time be consistent with the naming of classes whatever format you 
follow make sure it stays consistent through out the project











































 Recommendations
Architecture

Write a load_all_into_memory() function that loops through all records in File_manager and calls Vector_store::insert() — call it on server startup and on LOAD. This is the critical missing link between your two classes.
Define the response protocol clearly in a comment or doc: id score\n per result, terminated by END\n. Person B's Python client will depend on this.

Robustness

Add try-catch around std::stof / std::stoi in insert_parsing the same way query_parsing does — a bad float will currently throw and crash the server.
Add a bounds check in brute_force_search for empty stores.

For Next Steps (Week 3 prep)

Keep Vector struct and File_manager header extensible — metadata will need to be serialised alongside embeddings in Week 3, and the fixed record_size_ formula will need updating.

The most urgent thing to fix before moving on: the LOAD→memory bridge and QUERY response sending — without those two, the server can insert but never usefully answer a query across restarts.