// convert this all into a proper readme page 
// documnet all the commands needed to make this code run and work as intended
// it includes 2 window defender bypasses, 1 ip search etc 
requirements.txt:
openai>=1.0.0
python-dotenv>=1.0.0
pypdf>=3.0.0
python-docx>=1.0.0
streamlit>=1.32.0
Create a setup.txt or setup.md 

-------pipeline/embedder.py-------
Requirements: 
    1. 'ollama' installed
    2. Use 'ollama' to pull 'nomic-embed-text'
    3. Have 'dotenv' installed to use .env files
        for this you have to create a virtual env,
            python3 -m venv .venv       # Create the virtual env
            source .venv/bin/activate   # activate the venv
            pip install dotenv          # Install the package
            deactivate                  # To exit the venv

-------pipeline/ingestor.py-------
Requirements:
    1. pip install pypdf
    2. pip install python-docx

-------app_gui.py-------
Requirements:
    1. pip install streamlit


----architechture problems-----
1. How does a client join externally ? 
2. I would have to make the server go online somehow ? 
3. Note: The server is online on a local network, 
4. Problem: TO connect we expect the client to have some level of code, like the client.py at the very least 
5. What is the solution ? 
6. Make the server go online is the best solution
7. What does that even mean ? 
8. Make it just a online website anyone can visit
9. That would require what, what changes ? 
10. The UI is run locally , even if the server can listen , the UI is absent , how will the client intract ?

==============================================Chnage later=========================
New-NetFirewallRule -DisplayName "Ollama WSL Allow" -Direction Inbound -LocalPort 11434 -Protocol TCP -Action Allow
ip route | grep default




Project Root/
├── app/
│   └── rag_chatbot.py          # Orchestrates retrieval context and local LLM chat loop
├── client/
│   └── vecdb_client.py         # Handles low-level TCP serialization and network protocol constraints
├── data/
│   ├── documnets/              # Directory form where files are ingested into the rag_chatbot
│   ├── chunk_store.json        # Local JSON cache mapping vector IDs to raw text chunks
│   └── database.vdb            # Persistent binary file containing raw vector records and headers
├── pipeline/
│   ├── embedder.py             # Integrates Ollama backend for mxbai-embed-large (1024-d) execution
│   ├── ingestor.py             # Document parser and sliding-window text-chunking engine
│   └── searcher.py             # Independent search utility for direct database querying
├── include/
    ├── command_parser.h
    ├── file_manager.h
    ├── ivf.h
    ├── similarities.hpp            # Inlined math execution routines for L2 distance and dot products
    ├── types.h                     # Data primitives including Vector, Metadata_entry, and Query_result
    ├── vector_server.h
    └── vector_store.h
├── src/  
    ├── command_parser.cpp          # Request validation and token parsing logic
    ├── file_manager.cpp            # Low-level disk I/O handling the 32-byte aligned database header
    ├── ivf.cpp                     # K-Means clustering algorithm and centroid list management
    ├── main.cpp                    # System entry point instantiating the database layers and server socket
    ├── vector_server.cpp           # Direct socket handling loop (binds to port 8080)
    └── vector_store.cpp            # RAM-allocated flat array structures handling runtime query sorting
├── tests/                      # Tests for individual files
├── app_gui.py                  # Streamlit orchestration script launching the presentation layer dashboard
├── .env.example                # Pre-Set values and paths
├── Cmakelists.txt              # Cmake configration file
├── Readme.md                       
├── Dockerfile.cpp              # Docker image config file for C++ server
├── Dockerfile.python           # Docker image config file for python side
└── docker-compose.yml          # Docker compose file 
